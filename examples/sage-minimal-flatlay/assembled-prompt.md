# Assembled Prompt — Synthetic Bracelet Control

Status: documentation fixture; no generation has been run.

## Roles

- Product source: an admitted synthetic transparent bracelet cutout, when available.
- Style reference: an admitted sage minimal surface reference, when available.
- Product Module: `bracelets`.
- Style Card: `sage-minimal-flatlay`.
- Output Profile: `ecommerce`.
- Mode: `SAFE RUN`.

## Source-first render brief

Edit from the original transparent bracelet source. Preserve exact circumference, resting geometry, opening, visible links or band edges, clasp, charm and stone count, spacing, metal color, finish, transparent components, highlights, and internal negative spaces. Never regularize the geometry or invent hidden structure.

Create a pale desaturated sage matte surface with subtle paper-like grain. Use broad soft upper-left light and a short diffused lower-right contact shadow. The new surface, grade, and reflected tint must not alter the product color or metal finish.

Use the source aspect ratio by default. For an explicitly requested alternate ratio, expand the background and preserve the product unchanged. Target approximately 15% clear padding when canvas dimensions make that possible. Add no prop, product, logo, caption, label, watermark, or typography from the reference.

Run source/reference/output QA and report evidence. This brief does not authorize a PASS before a real output exists.
